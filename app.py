import streamlit as st

# إعدادات الصفحة
st.set_page_config(page_title="Portfolio | AI & Data Science", layout="wide")

# القائمة الجانبية للتنقل
st.sidebar.title("التنقل")
page = st.sidebar.radio("اذهب إلى:", ["الرئيسية", "تقارير Power BI", "نماذج الذكاء الاصطناعي"])

if page == "الرئيسية":
    st.title("👋 أهلاً بك في موقعي الاحترافي")
    st.markdown("### أنا عالم بيانات ومهندس ذكاء اصطناعي")
    st.write("هنا أعرض مشاريعي التي تجمع بين تحليل البيانات والحلول الذكية.")

elif page == "تقارير Power BI":
    st.title("📊 لوحات تحكم Power BI التفاعلية")
    st.write("استكشف تحليل البيانات التفاعلي أدناه:")
    
    # الرابط الذي استخرجناه من الكود الخاص بك
    power_bi_url = "https://app.powerbi.com/reportEmbed?reportId=31eff9f7-0f50-406a-a3e9-ea1050b96daa&autoAuth=true&embeddedDemo=true"
    
    # استخدام مكون iframe لعرض التقرير بشكل احترافي
    st.components.v1.iframe(power_bi_url, height=600, scrolling=True)
    
    st.info("ملاحظة: إذا لم يظهر التقرير، قد يتطلب الأمر تسجيل الدخول لحساب Power BI الخاص بك.")

elif page == "نماذج الذكاء الاصطناعي":
    st.title("🤖 نموذج التنبؤ بأسعار العقارات")
    st.write("هذا النموذج يستخدم تعلم الآلة (Linear Regression) للتنبؤ بسعر العقار بناءً على المدخلات.")

    # إعداد مدخلات الزائر في أعمدة منسقة
    col1, col2 = st.columns(2)
    
    with col1:
        sqft = st.slider("المساحة (بالقدم المربع):", 500, 5000, 1500)
        rooms = st.selectbox("عدد الغرف:", [1, 2, 3, 4, 5])
    
    with col2:
        age = st.number_input("عمر العقار (بالسنوات):", 0, 50, 5)

    # منطق التنبؤ (معادلة تجريبية تحاكي نموذج AI)
    # في المشاريع الحقيقية، هنا نقوم بتحميل ملف النموذج model.pkl
    base_price = 50000
    prediction = (sqft * 150) + (rooms * 20000) - (age * 1500) + base_price

    st.markdown("---")
    st.subheader(f"💰 السعر المتوقع: ${prediction:,.2f}")
    
    # إضافة رسم بياني توضيحي
    import pandas as pd
    import plotly.express as px
    
    # إنشاء بيانات وهمية للعرض
    data = pd.DataFrame({
        'المساحة': [1000, 2000, 3000, 4000, 5000],
        'السعر': [200000, 350000, 500000, 650000, 800000]
    })
    fig = px.line(data, x='المساحة', y='السعر', title='العلاقة بين المساحة والسعر في النموذج')
    st.plotly_chart(fig)
